"""Monetization Platform: a deployable SaaS gateway that turns the four AI
agents in this repository into metered, paid API products.

The package is import-safe with no configuration: it defaults to a local SQLite
database, mock Stripe, and mock LLM providers so it runs end-to-end with no keys.
"""

__version__ = "0.1.0"
