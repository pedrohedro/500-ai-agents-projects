# Core pipeline runs on the Python standard library alone (mock mode needs none
# of these). The pins below enable the OpenAI provider, the FastAPI webhook, the
# Stripe checkout, and the test suite.

# --- LLM provider (optional; only needed for LLM_PROVIDER=openai) ---
openai==1.51.2

# --- Web / automation layer (optional; only needed for the webhook API) ---
fastapi==0.115.2
uvicorn==0.30.6
pydantic==2.9.2

# --- Config ---
python-dotenv==1.0.1

# --- Billing (optional; only needed when STRIPE_API_KEY is set) ---
stripe==10.12.0

# --- Testing ---
pytest==8.3.3
httpx==0.27.2
